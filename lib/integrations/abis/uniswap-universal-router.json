⚠️ FILL IN — paste Uniswap's Universal Router real ABI (from the @uniswap/universal-router package for the version deployed on chain 4663) here as a JSON array. lib/cirq-config rejects this file until it is.
